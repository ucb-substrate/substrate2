[package]
name = "verilog"
version = "0.2.2"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
scir = { version = "0.9.2", registry = "substrate", path = "../scir" }
