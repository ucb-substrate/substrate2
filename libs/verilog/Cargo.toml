[package]
name = "verilog"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
scir = { version = "0.7.1", registry = "substrate", path = "../scir" }
