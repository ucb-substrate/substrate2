[package]
name = "nutlex"
version = "0.1.0"
edition = "2021"
description = "Parse SPICE nutmeg files (aka rawfiles)"

[dependencies]
nom = "7.1"
serde = { version = "1", features = ["derive"] }
thiserror = "1"
