[package]
name = "uniquify"
version = "0.4.0"
edition = "2024"
description = "A library for assigning unique names."
license = "BSD-3-Clause"

[dependencies]
arcstr = { version = "1", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
