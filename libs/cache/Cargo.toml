[package]
name = "cache"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
once_cell = "1"

[dev-dependencies]
crossbeam-channel = "0.5"
anyhow = "1"
