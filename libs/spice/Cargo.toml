[package]
name = "spice"
version = "0.3.0"
edition = "2021"

[dependencies]
scir = { version = "0.4.0", registry = "substrate", path = "../scir" }
opacity = { version = "0.1.0", path = "../opacity", registry = "substrate" }
arcstr = { version = "1", features = ["serde", "substr-usize-indices"] }
nom = "7"
thiserror = "1"
tracing = "0.1"
