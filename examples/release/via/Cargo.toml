[package]
name = "via"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
substrate = { version = "0.10.3", registry = "substrate" }
layir = { version = "0.2.2", registry = "substrate" }
