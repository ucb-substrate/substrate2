[package]
name = "sky130_inverter"
version = "0.0.0"
edition = "2021"
publish = false

# begin-code-snippet dependencies
[dependencies]
substrate = { version = "0.10.1", registry = "substrate" }
sky130 = { version = "0.10.1", registry = "substrate" }
layir = { version = "0.2.1", registry = "substrate" }
gdsconv = { version = "0.2.1", registry = "substrate" }
gds = { version = "0.4.0", registry = "substrate", path = "../../../libs/gds" }
scir = { version = "0.9.0", registry = "substrate", path = "../../../libs/scir" }
spice = { version = "0.9.1", registry = "substrate" }

arcstr = "1"
rust_decimal = "1"
rust_decimal_macros = "1"
# end-code-snippet dependencies

# begin-code-snippet open-dependencies
ngspice = { version = "0.5.1", registry = "substrate" }
magic_netgen = { version = "0.1.2", registry = "substrate" }
magic = { version = "0.2.1", registry = "substrate" }
# end-code-snippet open-dependencies

# begin-code-snippet cds-dependencies
spectre = { version = "0.11.1", registry = "substrate" }
quantus = { version = "0.2.1", registry = "substrate" }
pegasus = { version = "0.2.0", registry = "substrate", path = "../../../tools/pegasus" }
# end-code-snippet cds-dependencies
