[package]
name = "colbuf"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
substrate = { version = "0.9.0", registry = "substrate" }
spice = { version = "0.8.0", registry = "substrate" }
spectre = { version = "0.10.0", registry = "substrate" }
ngspice = { version = "0.4.0", registry = "substrate" }
quantus = { version = "0.1.0", registry = "substrate" }
magic_netgen = { version = "0.1.0", registry = "substrate" }
rust_decimal = "1.36.0"
rust_decimal_macros = "1.36.0"

[dev-dependencies]
approx = "0.5"
