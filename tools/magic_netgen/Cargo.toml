[package]
name = "magic_netgen"
version = "0.1.0"
edition = "2021"

[dependencies]
substrate = { version = "0.9.0", path = "../../substrate" }
scir = { version = "0.8.0", path = "../../libs/scir" }
spice = { version = "0.8.0", path = "../../libs/spice" }
magic = { version = "0.1.0", registry = "substrate", path = "../magic" }
netgen = { version = "0.1.0", registry = "substrate", path = "../netgen" }
